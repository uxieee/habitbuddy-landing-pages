# Habit Buddy QA Report

- UTC timestamp: 2026-02-20T22:48:49Z
- Workspace: `/Users/uxie/Documents/Vibe Code/Habit Buddy/landing-pages`
- QA mode: full-stack regression + targeted fixes + MCP crosschecks (read-only)

## Scope Covered

1. Frontend form UX/validation and responsiveness (`maxsupport.html`)
2. Backend payload validation + CRM/Stripe flow integrity (`functions/_lib/habitbuddy.js`)
3. API surface smoke checks (status/method handling/origin enforcement)
4. Automated tests (`node --test tests/*.mjs`)
5. Security and perf spot checks on production URL
6. Stripe + GHL crosschecks via MCP (read-only)

## User-Requested Changes Implemented

1. Required fields now enforced for main signup form:
   - `first_name` required
   - `last_name` required
   - `email` required
   - `phone` required (existing behavior preserved)
2. Name changed from single field to first + last fields in UI.
3. Responsive form layout improved using mobile-first grid + container query behavior.
4. Backend validation aligned with frontend requirements so API cannot accept incomplete lead payloads.

## Code Changes

1. `maxsupport.html`
   - Added `first_name` + `last_name` input fields.
   - Made `email` explicitly required and removed optional label.
   - Added responsive layout primitives:
     - fluid padding/margins with `clamp()`
     - container context via `container-type: inline-size`
     - container query for 2-column name row at larger form widths
   - Updated bottom CTA focus target to `firstName`.

2. `functions/_lib/habitbuddy.js`
   - `normalizeMainLeadPayload` now requires `last_name` and required `email`.
   - Added `formatDisplayName()` helper and propagated full-name usage through:
     - contact upsert
     - opportunity naming
     - Stripe customer creation
     - metadata/session fallback paths for post-checkout processing

3. `tests/api-endpoints.test.mjs`
   - Added API validation regression test verifying required main fields are enforced before any upstream calls.

4. `tests/maxsupport-form.test.mjs` (new)
   - Added frontend regression tests verifying:
     - required first/last/email/phone fields are present
     - email is no longer optional
     - responsive container-query name-row behavior exists
     - bottom CTA focuses `firstName`

## Automated Test Results

Command:

```bash
node --test tests/*.mjs
```

Result: PASS (11/11)

- API/security tests: PASS
- Webhook tests: PASS
- New field-requirement regression test: PASS
- New responsive/form-structure regression test: PASS

## Production Smoke/API/Security/Perf Spot Checks

Target: `https://habitbuddy-landing-pages.pages.dev`

1. Smoke/API:
   - `GET /api/health` -> 200
   - `GET /api/security-config` -> 200
   - `GET /api/main-payment-element-init` -> 405
   - `OPTIONS /api/main-payment-element-init` -> 204
   - `GET /api/gift-payment-element-init` -> 405
   - `OPTIONS /api/gift-payment-element-init` -> 204

2. Validation behavior (production):
   - POST main payment init with missing phone -> `{"success":false,"error":"Phone number is required."}`
   - POST gift payment init without plan -> `{"success":false,"error":"Gift plan is required."}`

3. Security headers on `/` include:
   - `content-security-policy`
   - `permissions-policy`
   - `referrer-policy`
   - `x-content-type-options`
   - `x-frame-options`

4. Perf spot timings (seconds):
   - `/` 0.069302
   - `/maxsupport.html` 0.044414
   - `/giftahabitbuddy.html` 0.054812
   - `/api/health` 0.071738
   - `/api/security-config` 0.038117

## MCP Crosschecks (Read-Only)

### Stripe (read-only)

1. Recent subscription check:
   - `sub_1T32GVAuSIQTND92tSOEIRi7` status `trialing`
2. Recent gift payment intents check:
   - `pi_3T32HUAuSIQTND920rSwIguK` (1m) status `succeeded` amount 2999
   - `pi_3T32IRAuSIQTND921W8Nbec9` (3m) status `succeeded` amount 7999
   - `pi_3T32JDAuSIQTND920r8YfY91` (6m) status `succeeded` amount 13999

### GHL (read-only)

Pipeline map verified for location `3ouY0YkB0fLDFs5nb8UG`, pipeline `fQzLboVKi639klKBI64N`:

1. Main trial opportunity
   - `YHFoA7kXEDXuULg6ZTGI` in stage `29d60203-9e41-4678-80db-4b96b9c17fbf` (Max Support Trial)
2. Gift 1m
   - `pw8wC74Ftq7e2XApd2lY` in stage `7715d018-dcba-4037-b261-aa0f7ffeba83`
3. Gift 3m
   - `5ib1Nu8WCj1IuwhzOQiW` in stage `62071a34-2e22-41a6-8fcb-450ea698f29e`
4. Gift 6m
   - `gtGKC5gRjT1A5dC851g7` in stage `a637ae2a-2ac4-4657-863e-576d3d0ea727`

## Bugs Found and Fixed

1. Main signup accepted only first name (no last name field).
   - Fixed by adding required `last_name` UI + backend validation.
2. Main signup email was optional in UI and backend.
   - Fixed by making email required in UI + backend.
3. Potential name inconsistency between frontend, Stripe customer, and GHL records.
   - Fixed by propagating full-name handling and metadata fallbacks.
4. Form responsiveness for expanded name fields needed explicit adaptive layout.
   - Fixed via container-query-driven two-column name row and mobile-first fallback.

## Deployment Note

- Production URL checks reflect currently deployed build.
- Code changes in this report are committed in repo; deploy is required for production UI/API to reflect new first/last/email requirements.

## Dead Code Review

- No safe dead-code removals were identified in touched paths during this pass.

## Overall Status

- Local code QA status: PASS
- Regression tests: PASS
- MCP crosschecks: PASS (read-only)
- Remaining action: deploy updated commit to production to activate new UI/backend rules live.
